server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      hostname: localhost

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: utenteervice
          uri: lb://utenteService
          predicates:
            - Path=/utente/**
        - id: automobileService
          uri: lb://automobile-service
          predicates:
            - Path=/automobile/**
        - id: moto-service
          uri: lb://moto-service
          predicates:
            - Path=/moto/**

